//*to cal factorial of a number
#include<stdio.h>
int main()
{

    int fact=1, n;

    printf("Enter a number to find factorial: ");
    scanf("%d",&n); 

    for(int i=1; i<=n; i++)
    {
        fact=fact*x; 
     }
    
    printf("Factorial of %d is: %d",n,fact);

    return 0;

}
